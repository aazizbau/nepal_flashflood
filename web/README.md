# Before/after map

Place the deployable slider-map application here. Generated tiles and GeoJSON
belong in `web/data/` and are ignored by Git by default. Record imagery licenses
and attribution requirements before publishing any layer.

